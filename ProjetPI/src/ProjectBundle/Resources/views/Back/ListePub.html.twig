{% extends 'EspaceAdmin.html.twig' %}

{% block content %}

    <div class="col-lg-12 stretch-card">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title">Gestion des Publication</h4>
                <div class="col-sm-6 col-md-4 col-lg-3"> <a href="{{ path('ajouterPub') }}"> <i class="ti-plus"></i> Ajouter Publication</a></div>

                <div class="table-responsive pt-3">
                    <table class="table table-bordered">
                        <thead>
                        <tr class="table-danger">
                            <th>Réference</th>
                            <th>Titre</th>
                            <th>Contenu</th>
                            <th>Actions</th>

                        </tr>
                        </thead>
                        <tbody>
                        {% for r in publication %}
                            <tr class="table-info">
                                <td>{{ r.id }}</td>
                                <td>{{ r.titre }}</td>
                                <td>{{ r.contenu }}</td>


                                <td>
                                    <a href="{{ path('modifierPublication', {'id': r.id}) }}"> Modifier</a>
                                    <a href="{{ path('supprimerPublication', {'id': r.id}) }}" >Supprimer</a>
                                </td>
                            </tr>
                        {% endfor %}
                        </tbody>
                    </table>
                </div>
            </div>
        </div>
    </div>
    </div>
    </div>
{% endblock %}