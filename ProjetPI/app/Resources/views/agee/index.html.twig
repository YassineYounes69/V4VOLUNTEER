{% extends 'base.html.twig' %}

{% block content %}
    <h1>Agees list</h1>

    <table>
        <thead>
            <tr>
                <th>Cin</th>
                <th>Nom</th>
                <th>Prenom</th>
                <th>Age</th>
                <th>Adresse</th>
                <th>Type</th>
                <th>Actions</th>
            </tr>
        </thead>
        <tbody>
        {% for agee in agees %}
            <tr>
                <td><a href="{{ path('agee_show', { 'cin': agee.cin }) }}">{{ agee.cin }}</a></td>
                <td>{{ agee.nom }}</td>
                <td>{{ agee.prenom }}</td>
                <td>{{ agee.age }}</td>
                <td>{{ agee.adresse }}</td>
                <td>{{ agee.type }}</td>
                <td>
                    <ul>
                        <li>
                            <a href="{{ path('agee_show', { 'cin': agee.cin }) }}">show</a>
                        </li>
                        <li>
                            <a href="{{ path('agee_edit', { 'cin': agee.cin }) }}">edit</a>
                        </li>
                    </ul>
                </td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('agee_new') }}">Create a new agee</a>
        </li>
    </ul>
{% endblock %}
